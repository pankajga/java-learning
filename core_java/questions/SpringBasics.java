package questions;

/* Annotation injection is performed before XML injection. Thus, XML config will override that of annotation.
 * ResponseEntity<Foo> response = restTemp.exchange(url, HTTPMethod.POST, request, Foo.class);
 */

public class SpringBasics {

}
